rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Grant full read/write access to any path for authenticated users.
    // This removes all ambiguity for nested collections like /live_sessions/{id}/participants
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}